<Page
    x:Class="DispensaryLabel.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DispensaryLabel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Spacing="10" Padding="20">

        <TextBlock Text="Company Name" FontWeight="Bold"/>
        <TextBox x:Name="CompanyName" PlaceholderText="Enter company name" Width="300" HorizontalAlignment="Left" Margin="0,0,0,20"/>

        <TextBlock Text="Company Address" FontWeight="Bold"/>
        <TextBox x:Name="CompanyAddress" PlaceholderText="Enter company address" Width="300" HorizontalAlignment="Left" Margin="0,0,0,20"/>

        <TextBlock Text="Strains CSV File" FontWeight="Bold" />
        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,20" >
            <TextBox x:Name="CsvFilePath" IsReadOnly="True" PlaceholderText="No file selected" Width="300"/>
            <Button Content="Browse" Click="BrowseCsv_Click"/>
        </StackPanel>

        <TextBlock Text="Printer Select" FontWeight="Bold"/>
        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,20">
            <ComboBox x:Name="PrinterList" PlaceholderText="Select Printer" Width="200"/>
            <Button Content="Test Printer" Click="TestPrinter_Click"/>
        </StackPanel>

        <TextBlock Text="Scale COM Port" FontWeight="Bold"/>
        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,20">
            <ComboBox x:Name="ScaleComPorts" PlaceholderText="Select COM port" Width="200"/>
            <Button Content="Test Scale" Click="TestScale_Click"/>
        </StackPanel>

        <!-- Added Dark Mode Toggle -->
        <TextBlock Text="Dark Mode" FontWeight="Bold"/>
        <ToggleSwitch x:Name="DarkModeToggle" OffContent="Light" OnContent="Dark" Toggled="DarkModeToggle_Toggled"/>

        <Button Content="Save Settings" Click="SaveSettings_Click"/>
    </StackPanel>
</Page>